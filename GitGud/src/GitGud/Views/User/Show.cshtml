@model GitGud.Models.User
@{
    ViewBag.Title = "Show profile";
}

@section scripts {
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
    <script type="text/javascript" src="~/js/script.js"></script>

}

<div class="container">
    <div class="row">
        <h2 style="text-align:center">User <strong>@Model.UserName</strong> - Profile Details</h2>
    </div>
    <div class="row">
        <div class="panel-group col-md-9" id="accordion">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h4 class="panel-title">
                        <a data-toggle="collapse" data-parent="#accordion" href="#collapse1">
                            User details
                        </a>
                    </h4>
                </div>
                <div id="collapse1" class="panel-collapse collapse in">
                    <div class="panel-body">
                        <div class="row">
                            <table class="table table-hover">
                                <tr>
                                    <th>Names: </th>
                                    <td>
                                        @if (!string.IsNullOrEmpty(Model.FirstName))
                                        {
                                            <strong>@Model.FirstName</strong>
                                        }
                                        @if (!string.IsNullOrEmpty(Model.LastName))
                                        {
                                            <strong>@Model.LastName</strong>
                                        }
                                        @if (string.IsNullOrEmpty(Model.FirstName) && string.IsNullOrEmpty(Model.LastName))
                                        {
                                            <strong>(n/a)</strong>
                                        }
                                    </td>
                                </tr>
                                <tr>
                                    <th>Lives in: </th>
                                    <td>
                                        @if (!string.IsNullOrEmpty(Model.Town))
                                        {
                                            <strong>@Model.Town</strong>
                                        }
                                        else
                                        {
                                            <strong>(n/a)</strong>
                                        }
                                    </td>
                                </tr>
                                <tr>
                                    <th>Birthday: </th>
                                    <td>
                                        @if (Model.Birthday.Year != 1)
                                        {
                                            <strong>@(Model.Birthday.Date.ToString("d"))</strong>
                                        }
                                        else
                                        {
                                            <strong>(n/a)</strong>
                                        }
                                    </td>
                                </tr>
                                <tr>
                                    <th>Age: </th>
                                    <td>
                                        @if (Model.Birthday.Year != 1)
                                        {
                                            <strong>@(DateTime.Now.Year - Model.Birthday.Year)</strong>
                                        }
                                        else
                                        {
                                            <strong>(n/a)</strong>
                                        }

                                    </td>
                                </tr>
                            </table>
                        </div>
                        <div id="edit">
                            <a class="btn btn-primary" asp-controller="User" asp-action="Edit">Edit profile</a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h4 class="panel-title">
                        <a data-toggle="collapse" data-parent="#accordion" href="#collapse2">
                            My songs
                        </a>
                    </h4>
                </div>
                <div id="collapse2" class="panel-collapse collapse">
                    <div class="panel-body">
                        <div class="row">
                            <table class="table table-hover">
                                @if (Model.Songs.Any())
                                {
                                    <thead>
                                        <tr>
                                            <th>Song</th>
                                            <th>Artist</th>
                                            <th>Date added</th>
                                            <th>Audio</th>
                                        </tr>
                                    </thead>
                                }
                                else
                                {
                                    <p style="text-align: center">No songs from user @User.Identity.Name available</p>
                                }
                                <tbody>
                                    @foreach (var song in Model.Songs)
            {
                                        <tr class="info">
                                            <td><a asp-controller="Song" asp-action="Details" asp-route-songId="@song.Id" style="margin:3px">@song.Name</a></td>
                                            <td><a asp-controller="Artist" asp-action="Artist" asp-route-artistName="@song.ArtistName" style="margin:3px">@song.ArtistName</a></td>
                                            <td>@song.DateUploaded.ToString("d")</td>
                                            <td>
                                                <audio controls preload="none">
                                                    <source src="~/uploads/@song.fileAdress" type="audio/ogg">
                                                    Your browser does not support the audio element.
                                                </audio>
                                            </td>
                                            <td>
                                                <a asp-controller="Song" asp-action="UserDeleteSong" asp-route-strSongId="@song.Id" class="btn btn-primary">Delete</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h4 class="panel-title">
                        <a data-toggle="collapse" data-parent="#accordion" href="#collapse3">
                            My comments
                        </a>
                    </h4>
                </div>
                <div id="collapse3" class="panel-collapse collapse">
                    <div class="panel-body">
                        <div class="row">
                            <table class="table table-hover">
                                @if (Model.Comments.Any())
                                {
                                    <thead>
                                        <tr>
                                            <th>Content</th>
                                            <th>DateAdded</th>
                                            <th>Likes Count</th>
                                        </tr>
                                    </thead>
                                }
                                else
                                {
                                    <p style="text-align: center">No comments from user @User.Identity.Name available</p>
                                }
                                <tbody>
                                    @foreach (var comment in Model.Comments)
            {
                                        <tr class="info">

                                            <td>@comment.Content</td>
                                            <td>@comment.Date</td>
                                            <td>@comment.Likes.Count</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h4 class="panel-title">
                        <a data-toggle="collapse" data-parent="#accordion" href="#collapse4">
                            My favorites (songs that I've liked)
                        </a>
                    </h4>
                </div>
                <div id="collapse4" class="panel-collapse collapse">
                    <div class="panel-body">
                       To do... or maybe not...
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="thumbnail">
                <a href="~/images/pic3.jpg" target="_blank">
                    <img src="~/images/pic3.jpg" alt="Fjords" style="width: 100%">
                </a>
                <div class="caption">
                    <p><strong>Username: @User.Identity.Name</strong></p>
                    <p><strong>Email: @Model.Email</strong></p>
                </div>
            </div>
        </div>
    </div>
</div>